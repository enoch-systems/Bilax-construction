"use client";

// Disabled - was causing stiff scrolling by hijacking native scroll
// Native browser scrolling is now active for better performance
export default function SmoothScroll() {
  return null;
}
